import React from 'react'; // Make sure to import React
import NoteManager from './components/NoteManager'; // Adjust path if NoteManager.js is in a different folder

function App() {
    return (
        <div>
            <NoteManager />
        </div>
    );
}

export default App;
